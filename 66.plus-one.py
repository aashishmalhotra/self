def plusOne(digits: list[int]) -> list[int]:
    
    dig = ""
    for ele in digits:
        dig = dig + str(ele)
    #Making dig integer
    dig = int(dig) + 1

    # Converting back to string
    dig = 


digits = [1,2,3]
plusOne(digits)